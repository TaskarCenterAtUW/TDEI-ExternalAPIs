name: ExternalAPI
on:
  push:
    branches:
      - "feature-466"
    inputs:
      trigger_workflow:
        description: 'ًWorkflow to be triggered'
        required: true
        default: 'generate_api_client'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Test
        run: |
          curl -X POST https://api.github.com/repos/TaskarCenterAtUW/TDEI-api-client/dispatches \
          -H 'Accept: application/vnd.github.everest-preview+json' \
          -u ${{ secrets.ACTIONS_KEY }} \
          --data '{"event_type": $ {{ github.event.inputs.trigger_workflow }}, "client_payload": { "repository": "'"$GITHUB_REPOSITORY"'" }}'
      - uses: actions/checkout@v3
